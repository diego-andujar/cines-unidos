import { MovieListResponse } from './movie-list-response';

describe('MovieListResponse', () => {
  it('should create an instance', () => {
    expect(new MovieListResponse()).toBeTruthy();
  });
});
