import { MovieDetailResponse } from './movie-detail-response';

describe('MovieDetailResponse', () => {
  it('should create an instance', () => {
    expect(new MovieDetailResponse()).toBeTruthy();
  });
});
